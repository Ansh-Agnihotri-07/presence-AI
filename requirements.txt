# ─── Presence AI — Phase 1.5 Dependencies ───

# Core
python-dotenv>=1.0.0
httpx>=0.27.0

# UI
PyQt6>=6.6.0
qasync>=0.27.0

# LLM (cloud fallback)
openai>=1.12.0

# Voice (optional — install only if voice is needed)
faster-whisper>=0.10.0
edge-tts>=6.1.0
pyttsx3>=2.90
sounddevice>=0.4.6
soundfile>=0.12.0
webrtcvad-wheels>=2.0.10
numpy>=1.24.0

# Screen
mss>=9.0.0
pytesseract>=0.3.10
Pillow>=10.0.0
